import SettingsTitle from "../SettingsTitle";

const Social = props => {
    return (
        <>
            <SettingsTitle>Social Media Accounts</SettingsTitle>
        </>
    );
};

export default Social;